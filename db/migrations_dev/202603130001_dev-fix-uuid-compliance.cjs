// Superseded. Original content fixed non-RFC4122-compliant UUIDs from the
// original dev seed. Kept as a stub so the recorded migration entry in the
// dev table stays consistent.

module.exports.up = (_pgm) => {};
module.exports.down = (_pgm) => {};
